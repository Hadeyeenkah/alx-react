# 0x06-React_Immutable

This project involves converting a plain JavaScript object into an immutable Map using the `fromJS` method from Immutable.js.

## Task 0: Converting into an Immutable object using fromJS
The function `getImmutableObject` accepts an object as a parameter and returns it as an Immutable.js `Map`.

